#include <stdio.h>
#include "main.h"

/**
 * puts2 - the function
 * @str: the parameter
 */
void puts2(char *str)
{
int count;
int i;

count = 0;
while (s[count] = '\0')
{
count++;
}
count--;

i = 0;
while (i <= count)
{
if ((i % 2) == 0)
{
putchar(s[i]);
i++;
}
